import AsyncStorage from '@react-native-async-storage/async-storage'; import {STORAGE_KEYS} from '../utils/constants'; export async function saveLastGenerated(text){ try{ await AsyncStorage.setItem(STORAGE_KEYS.LAST_GEN,text);}catch(e){console.warn(e);} } export async function getLastGenerated(){ try{ return await AsyncStorage.getItem(STORAGE_KEYS.LAST_GEN);}catch(e){console.warn(e);return null;} }