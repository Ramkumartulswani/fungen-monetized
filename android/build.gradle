buildscript {
    repositories {
        google()
        mavenCentral()
        maven { url("$rootDir/../node_modules/react-native/android") }   // REQUIRED
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.1.1")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.10")
        classpath("com.facebook.react:react-native-gradle-plugin")       // MUST MATCH REPO ABOVE
    }
}

allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
        google()
        maven { url("$rootDir/../node_modules/react-native/android") }   // REQUIRED
    }
}
